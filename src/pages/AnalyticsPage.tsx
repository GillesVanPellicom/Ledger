import React, {useState, useEffect, useMemo, useRef, useCallback, useLayoutEffect} from 'react';import ReactECharts from 'echarts-for-react';import Select from '../components/ui/Select';import Card from '../components/ui/Card';import {  ArrowDown,  ArrowUp,  Calculator,  ShoppingBag,  Euro,  Info,  Minus,  LayoutDashboard,  Package,  Users} from 'lucide-react';import Spinner from '../components/ui/Spinner';import TopProducts from '../components/analytics/TopProducts';import {cn} from '../utils/cn';import Tooltip from '../components/ui/Tooltip';import InfoCard from '../components/ui/InfoCard';import {useNavigate} from 'react-router-dom';import { Header } from '../components/ui/Header';import PageWrapper from '../components/layout/PageWrapper';import { useAvailableYears, useAnalyticsData, useCategoryAnalytics } from '../hooks/useAnalytics';import { useSettingsStore } from '../store/useSettingsStore';import Divider from '../components/ui/Divider';import { Tabs, TabsList, TabsTrigger } from '../components/ui/Tabs';import MoneyDisplay from '../components/ui/MoneyDisplay';const AnalyticsPage: React.FC = () => {  const monthlyChartRef = useRef<ReactECharts>(null);  const storeChartRef = useRef<ReactECharts>(null);  const categoryChartRef = useRef<ReactECharts>(null);  const categoryBarChartRef = useRef<ReactECharts>(null);  const debtBarChartRef = useRef<ReactECharts>(null);  const navigate = useNavigate();  const [activeTab, setActiveTab] = useState('general');  const [selectedYear, setSelectedYear] = useState<string>(new Date().getFullYear().toString());  const [categoryYear, setCategoryYear] = useState<string | 'all'>(new Date().getFullYear().toString());  const [categoryMonth, setCategoryMonth] = useState<string | 'all'>('all');  const [categoryWeek, setCategoryWeek] = useState<string | 'all'>('all');  const {settings} = useSettingsStore();  const paymentMethodsEnabled = settings.modules.paymentMethods?.enabled;  const debtEnabled = settings.modules.debt?.enabled;  const isDarkMode = useMemo(() => document.documentElement.classList.contains('dark'), []);  const theme = isDarkMode ? 'dark' : 'light';  const { data: availableYears } = useAvailableYears();  const { data: analyticsData, isLoading } = useAnalyticsData(selectedYear, paymentMethodsEnabled, debtEnabled);  const { data: categoryData, isLoading: isCategoryLoading } = useCategoryAnalytics(String(categoryYear), categoryMonth === 'all' ? null : categoryMonth, categoryWeek === 'all' ? null : categoryWeek);  useEffect(() => {    if (availableYears && availableYears.length > 0 && !availableYears.includes(selectedYear)) {      setSelectedYear(availableYears[0]);      setCategoryYear(availableYears[0]);    }  }, [availableYears, selectedYear]);  const monthlyChartOption = useMemo(() => ({    backgroundColor: 'transparent',    tooltip: {      trigger: 'axis',      valueFormatter: (value: number | string) => typeof value === 'number' ? `€${value.toFixed(2)}` : value    },    xAxis: {      type: 'category',      data: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']    },    yAxis: {type: 'value', axisLabel: {formatter: '€{value}'}},    series: [{data: analyticsData?.monthlySpending.map(m => m.total.toFixed(2)) || [], type: 'bar'}],    grid: {left: '3%', right: '4%', bottom: '3%', containLabel: true}  }), [analyticsData?.monthlySpending]);  const storeChartOption = useMemo(() => ({    backgroundColor: 'transparent',    tooltip: {trigger: 'item', formatter: '{b}: €{c} ({d}%)'},    legend: {orient: 'vertical', left: 'left', type: 'scroll'},    series: [{      name: 'Store Spending',      type: 'pie',      radius: '50%',      data: analyticsData?.storeSpending || [],      emphasis: {itemStyle: {shadowBlur: 10, shadowOffsetX: 0, shadowColor: 'rgba(0, 0, 0, 0.5)'}}    }]  }), [analyticsData?.storeSpending]);  const categoryChartOption = useMemo(() => ({    backgroundColor: 'transparent',    tooltip: {trigger: 'item', formatter: '{b}: €{c} ({d}%)'},    legend: {orient: 'vertical', left: 'left', type: 'scroll'},    series: [{      name: 'Category Spending',      type: 'pie',      radius: '50%',      data: categoryData?.categorySpending || [],      emphasis: {itemStyle: {shadowBlur: 10, shadowOffsetX: 0, shadowColor: 'rgba(0, 0, 0, 0.5)'}}    }]  }), [categoryData?.categorySpending]);  const categoryBarChartOption = useMemo(() => ({    backgroundColor: 'transparent',    tooltip: {      trigger: 'axis',      axisPointer: {type: 'shadow'},      formatter: (params: any) => `${params[0].name}: €${params[0].value.toFixed(2)}`    },    grid: {left: '3%', right: '4%', bottom: '3%', containLabel: true},    xAxis: {type: 'value', axisLabel: {formatter: '€{value}'}},    yAxis: {type: 'category', data: categoryData?.categorySpending.slice(0, 10).map(c => c.name).reverse() || []},    series: [{      name: 'Average Spend per Receipt',      type: 'bar',      label: {show: true, position: 'right', formatter: (params: any) => `€${params.value.toFixed(2)}`},      data: categoryData?.categorySpending.slice(0, 10).map(c => c.avg).reverse() || [],      itemStyle: { color: '#5470c6' }    }]  }), [categoryData?.categorySpending]);  const debtBarChartOption = useMemo(() => ({    backgroundColor: 'transparent',    tooltip: {      trigger: 'axis',      axisPointer: {type: 'shadow'},      formatter: (params: any) => `${params[0].name}: €${params[0].value.toFixed(2)}`    },    grid: {left: '3%', right: '4%', bottom: '3%', containLabel: true},    xAxis: {type: 'category', data: analyticsData?.debtStats.netBalances.map(d => d.name) || []},    yAxis: {type: 'value', axisLabel: {formatter: '€{value}'}},    series: [{      name: 'Net Balance',      type: 'bar',      label: {show: true, position: 'top', formatter: (params: any) => `€${params.value.toFixed(2)}`},      data: analyticsData?.debtStats.netBalances.map(d => ({        value: d.value,        itemStyle: {color: d.value >= 0 ? '#91cc75' : '#ee6666'}      })) || [],    }]  }), [analyticsData?.debtStats.netBalances]);  const TrendIndicator = ({current, previous}: { current: number, previous: number }) => {    if (previous === 0 || current === 0) return <span className="text-text-disabled">-</span>;    const change = ((current - previous) / previous) * 100;    const diff = current - previous;    const isUp = change > 0;    const colorClass = isUp ? 'text-red' : 'text-green';    return (<div className="flex flex-col items-center">      <span className={`flex items-center text-xs font-medium ${colorClass}`}>{isUp ?        <ArrowUp className="h-3 w-3 mr-0.5"/> :        <ArrowDown className="h-3 w-3 mr-0.5"/>}{Math.abs(change).toFixed(0)}%</span><span className={`text-[10px] ${colorClass}`}>{diff > 0 ? '+' : ''}€{diff.toFixed(0)}</span>    </div>);  };  const CardHeader = ({title, tooltipText}: { title: string, tooltipText: string }) => (    <div className="flex items-center gap-2 mb-4">      <h3 className="text-lg font-semibold text-font-1">{title}</h3>      <Tooltip content={tooltipText}><Info className="h-5 w-5 text-font-2"/></Tooltip>    </div>  );  const renderTabs = () => {    return (      <Tabs value={activeTab} onValueChange={(val: any) => setActiveTab(val)}>        <TabsList>          <TabsTrigger value="general">            <LayoutDashboard className="h-4 w-4" />            General          </TabsTrigger>          {!analyticsData?.onlyNonItemised && (            <TabsTrigger value="products">              <Package className="h-4 w-4" />              Products            </TabsTrigger>          )}          {debtEnabled && (            <TabsTrigger value="debt">              <Users className="h-4 w-4" />              Debt            </TabsTrigger>          )}        </TabsList>      </Tabs>    );  };  const gridContainerRef = useRef<HTMLDivElement>(null);  const [itemsToRender, setItemsToRender] = useState(9);  const calculateGridItems = useCallback(() => {    if (!gridContainerRef.current) return;    const containerHeight = gridContainerRef.current.offsetHeight;    if (containerHeight === 0) return;    const itemHeight = 88;    const windowWidth = window.innerWidth;    let cols = 1;    if (windowWidth >= 1024) { // lg      cols = 3;    } else if (windowWidth >= 640) { // sm      cols = 2;    }    if (itemHeight > 0) {      const rowsThatFit = Math.floor(containerHeight / itemHeight);      const rowsToRender = rowsThatFit + 1;      setItemsToRender(rowsToRender * cols);    }  }, []);  useLayoutEffect(() => {    const timer = setTimeout(() => {      calculateGridItems();    }, 100);    window.addEventListener('resize', calculateGridItems);    return () => {      clearTimeout(timer);      window.removeEventListener('resize', calculateGridItems);    };  }, [calculateGridItems, analyticsData?.paymentMethodStats.methods]);  const renderPaymentMethodGrid = () => {    const methods = analyticsData?.paymentMethodStats.methods || [];    const totalMethods = methods.length;    const windowWidth = typeof window !== 'undefined' ? window.innerWidth : 1024;    let cols = 1;    if (windowWidth >= 1024) { // lg      cols = 3;    } else if (windowWidth >= 640) { // sm      cols = 2;    }    const numToRender = Math.max(itemsToRender, totalMethods);    const displayCount = Math.ceil(numToRender / cols) * cols;    const items = Array.from({ length: displayCount }, (_, i) => methods[i] || null);    return (      <div ref={gridContainerRef} className="relative lg:col-span-3 border-t lg:border-t-0 border-border">        <div className="absolute inset-0 overflow-y-auto overflow-x-hidden">          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 -m-px">            {items.map((method, index) => (              <div                key={method ? method.id : `placeholder-${index}`}                className={cn(                  "p-4 flex flex-col items-center justify-center text-center border border-border h-[88px]",                  { "cursor-pointer transition-transform hover:scale-105": !!method },                )}                onClick={() => method && navigate(`/payment-methods/${method.id}`)}              >                {method ? (                  <>                    <span className="font-medium text-font-2 truncate">{method.name}</span>                    <MoneyDisplay amount={method.balance} className="text-lg font-semibold text-font-1" />                  </>                ) : (                  <Minus className="h-6 w-6 text-font-2" />                )}              </div>            ))}          </div>        </div>      </div>    );  };  return (    <div>      <Header title="Analytics" variant="tabs" tabs={renderTabs()} />      <PageWrapper>        <div className="py-6 space-y-6">          {activeTab === 'general' && (            <>              {paymentMethodsEnabled && (                <div className="pt-2">                  <div className="rounded-xl border border-border shadow-sm overflow-hidden">                    <div className="grid grid-cols-1 lg:grid-cols-3">                      {renderPaymentMethodGrid()}                    </div>                  </div>                </div>              )}              <div className="flex items-center justify-end">                <div className="w-48"><Select value={selectedYear}                                              onChange={(e: React.ChangeEvent<HTMLSelectElement>) => setSelectedYear(e.target.value)}                                              options={(availableYears || []).map(y => ({value: y, label: y}))}/></div>              </div>              <Card>                <div className="p-6">                  <CardHeader title="Monthly Spending" tooltipText={`Total spending for each month in ${selectedYear}.`}/>                  {isLoading ? <div className="flex justify-center items-center h-[300px] w-full"><Spinner/></div> :                    <ReactECharts option={monthlyChartOption}                                  theme={theme}                                  style={{height: '300px'}}                                  notMerge={true}/>}                </div>              </Card>              <Card>                <div className="p-4 overflow-x-auto">                  <table className="w-full text-sm text-center">                    <thead>                    <tr>                      <th className="p-2 text-left text-font-2 font-medium w-24"></th>                      {['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'].map(m => (                        <th key={m} className="p-2 font-medium text-font-1 min-w-[60px]">{m}</th>))}                    </tr>                    </thead>                    <tbody className="divide-y divide-border">                    <tr>                      <td className="p-2 text-left font-medium text-font-2">                        <div className="flex items-center gap-1">vs                                                                 Prev <Tooltip content="Compares spending to the previous month."><Info                            className="h-4 w-4 text-font-2"/></Tooltip></div>                      </td>                      {analyticsData?.monthlySpending.map((month, i) => (                        <td key={i} className="p-2"><TrendIndicator current={month.total} previous={month.prevMonthTotal}/>                        </td>))}                    </tr>                    <tr>                      <td className="p-2 text-left font-medium text-font-2">                        <div className="flex items-center gap-1">vs                                                                 Year <Tooltip content="Compares spending to the same month in the previous year."><Info                            className="h-4 w-4 text-font-2"/></Tooltip></div>                      </td>                      {analyticsData?.monthlySpending.map((month, i) => (                        <td key={i} className="p-2"><TrendIndicator current={month.total} previous={month.prevYearMonthTotal}/>                        </td>))}                    </tr>                    </tbody>                  </table>                </div>              </Card>              <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">                <Card>                  <div className="p-6">                    <CardHeader title="Spending by Store"                                tooltipText={`Breakdown of total spending per store for ${selectedYear}.`}/>                    {isLoading ? <div className="flex justify-center items-center h-[300px] w-full"><Spinner/></div> :                      <ReactECharts ref={storeChartRef}                                    option={storeChartOption}                                    theme={theme}                                    style={{height: '300px'}}                                    notMerge={true}/>}                  </div>                </Card>              </div>            </>          )}          {activeTab === 'products' && (            <div className="pt-6 space-y-6">              <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">                <div className="space-y-6">                  {analyticsData?.hasItemlessReceipts && (                    <InfoCard                      variant="warning"                      title="Data Accuracy"                      message="The following analytics may not be entirely accurate as there are one or more total-only expenses."                    />                  )}                  {analyticsData?.hasUncategorizedProducts && (                    <InfoCard                      variant="warning"                      title="Data Accuracy"                      message="The following analytics may not be entirely accurate as there is one or more products without a category."                    />                  )}                                    <TopProducts/>                </div>                <div className="space-y-6">                  <Card>                    <div className="p-6">                      <CardHeader title="Expense Averages"                                  tooltipText={`Average values calculated from all expenses in ${selectedYear}.`}/>                      <div className="space-y-6 mt-2">                        <div className="flex items-center justify-between p-4 bg-bg-2 border border-border rounded-xl">                          <div className="flex items-center gap-3">                            <div className="p-2 bg-blue/10 text-font-2 rounded-lg">                              <Euro className="h-6 w-6"/></div>                            <span className="font-medium text-font-1">Avg. Expense Total</span></div>                          <MoneyDisplay amount={analyticsData?.averages.avgPerReceipt || 0} showSign={false} colorPositive={false} colorNegative={false} className="text-xl font-bold text-font-1" />                        </div>                        <div className="flex items-center justify-between p-4 bg-bg-2 border border-border rounded-xl">                          <div className="flex items-center gap-3">                            <div className="p-2 bg-accent/10 text-font-2 rounded-lg">                              <ShoppingBag className="h-6 w-6"/></div>                            <span className="font-medium text-font-1">Avg. Items / Expense</span></div>                          <span className="text-xl font-bold text-font-1">{analyticsData?.averages.avgItemsPerReceipt.toFixed(1) || '0.0'}</span></div>                        <div className="flex items-center justify-between p-4 bg-bg-2 border border-border rounded-xl">                          <div className="flex items-center gap-3">                            <div className="p-2 bg-green/10 text-font-2 rounded-lg">                              <Calculator className="h-6 w-6"/></div>                            <span className="font-medium text-font-1">Avg. Price / Item</span></div>                          <MoneyDisplay amount={analyticsData?.averages.avgPricePerItem || 0} showSign={false} colorPositive={false} colorNegative={false} className="text-xl font-bold text-font-1" />                        </div>                      </div>                    </div>                  </Card>                  <Card>                    <div className="p-6">                      <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between mb-4 gap-4">                          <CardHeader title="Category Spending" tooltipText={`Breakdown of total spending per category.`}/>                          <div className="flex flex-wrap gap-2">                              <div className="w-32">                                  <Select value={String(categoryYear)} onChange={(e) => setCategoryYear(e.target.value)} options={[{value: 'all', label: 'All Years'}, ...(availableYears || []).map(y => ({value: y, label: y}))]} />                              </div>                              <div className="w-40">                                  <Select value={categoryMonth} onChange={(e) => { setCategoryMonth(e.target.value); setCategoryWeek('all'); }} options={[{value: 'all', label: 'All Months'}, ...Array.from({length: 12}, (_, i) => ({value: String(i + 1), label: new Date(0, i).toLocaleString('default', {month: 'long'})}))]} disabled={categoryYear === 'all'} />                              </div>                              <div className="w-32">                                  <Select value={categoryWeek} onChange={(e) => setCategoryWeek(e.target.value)} options={[{value: 'all', label: 'All Weeks'}, ...Array.from({length: 53}, (_, i) => ({value: String(i + 1), label: `Week ${i + 1}`}))]} disabled={categoryYear === 'all' || categoryMonth === 'all'} />                              </div>                          </div>                      </div>                                            {isCategoryLoading ? <div className="flex justify-center items-center h-[300px] w-full"><Spinner/></div> :                            <ReactECharts ref={categoryChartRef}                                          option={categoryChartOption}                                          theme={theme}                                          style={{height: '300px'}}                                          notMerge={true}/>                      }                    </div>                  </Card>                </div>              </div>            </div>          )}          {activeTab === 'debt' && debtEnabled && (            <div className="pt-2">              <div className="flex items-center gap-2 mb-4">                <h2 className="text-xl font-semibold text-font-1">Debt Analytics</h2>              </div>              <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">                <Card className="p-4 text-center bg-green/10 border-green/20">                  <p className="text-sm font-medium text-font-2">Total Owed to You</p>                  <MoneyDisplay amount={analyticsData?.debtStats.totalOwedToMe || 0} showSign={false} className="text-2xl font-bold text-font-1" />                </Card>                <Card className="p-4 text-center bg-red/10 border-red/20">                  <p className="text-sm font-medium text-font-2">Total You Owe</p>                  <MoneyDisplay amount={-(analyticsData?.debtStats.totalOwedByMe || 0)} showSign={false} className="text-2xl font-bold text-font-1" />                </Card>                <Card className={cn("p-4 text-center", ((analyticsData?.debtStats.totalOwedToMe || 0) - (analyticsData?.debtStats.totalOwedByMe || 0)) >= 0 ? "bg-blue/10 border-blue/20" : "bg-accent/10 border-accent/20")}>                  <p className="text-sm font-medium text-font-2">Overall Debt Balance</p>                  <MoneyDisplay                     amount={(analyticsData?.debtStats.totalOwedToMe || 0) - (analyticsData?.debtStats.totalOwedByMe || 0)}                     showSign={false}                     className="text-2xl font-bold text-font-1"                    colorPositive={false}                     colorNegative={false}                  />                </Card>              </div>              <Card>                <div className="p-6">                  <CardHeader title="Net Balance per Entity"                              tooltipText="Shows the net financial position with each entity. Positive values (green) mean they owe you, negative (red) mean you owe them."/>                  {isLoading ? <div className="flex justify-center items-center h-[400px] w-full"><Spinner/></div> :                    <ReactECharts ref={debtBarChartRef}                                  option={debtBarChartOption}                                  theme={theme}                                  style={{height: '400px'}}                                  notMerge={true}/>}                </div>              </Card>            </div>          )}        </div>      </PageWrapper>    </div>  );};export default AnalyticsPage;